# Skill Name

## When to use this skill
Describe the trigger conditions — what user requests, keywords, or contexts should cause the agent to load this skill.

- Use when the user asks to [do X]
- Use when the user mentions "[keyword]" or "[keyword2]"
- NOT for: [anti-patterns — what this skill should NOT handle]

## Setup (one-time)
Any installation steps required before the skill can run.

```bash
brew install some-tool
some-tool auth login
```

Store credentials at: `~/.config/my-tool/credentials.json`

## Usage

### Primary command
```bash
my-tool do-thing --option value
```

### Common patterns

**List items:**
```bash
my-tool list --format json
```

**Create:**
```bash
my-tool create --name "Example" --type project
```

**Update:**
```bash
my-tool update <id> --field value
```

## Output handling
- Parse JSON output with `python3 -m json.tool`
- Report result to user as a concise summary
- On error: [describe how to handle errors]

## Error handling
| Error | Resolution |
|-------|-----------|
| `Auth failed` | Re-run `some-tool auth login` |
| `Rate limit` | Wait 60s and retry |
| `Not found` | Verify the ID exists |

## Examples

**User asks:** "Do X for Y"
**Command:** `my-tool do-x --target y`
**Response:** "Done — [describe what happened]"
